apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-insights-sb.fullname" . }}
  labels:
    {{- include "app-insights-sb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-insights-sb.selectorLabels" . | nindent 4 }}
