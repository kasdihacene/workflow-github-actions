apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-insights-sb.fullname" . }}-test-connection"
  labels:
    {{- include "app-insights-sb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-insights-sb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
